import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as FieldStories from './Field.stories';

<Meta of={FieldStories} />

# Field

Field provides labels, hints, and validation messages for form inputs.

## Props

<Canvas of={FieldStories.Default} />

<Controls of={FieldStories.Default} />

## Validation State

A field can display different validation states to provide feedback to the user.

- `success`: indicates successful validation
- `warning`: indicates a warning that doesn't prevent submission
- `error`: indicates an error that should be corrected

<Canvas of={FieldStories.ValidationState} />

## Size

A field label can have different sizes.

<Canvas of={FieldStories.Size} />

## Orientation

A field can have different label orientations relative to the input.

<Canvas of={FieldStories.Orientation} />

## With Hint

A field can display hint text to provide additional guidance.

<Canvas of={FieldStories.WithHint} />

## Required

A field can be marked as required with an asterisk.

<Canvas of={FieldStories.Required} />

## Disabled

A field can be disabled to prevent user interaction.

<Canvas of={FieldStories.Disabled} />
