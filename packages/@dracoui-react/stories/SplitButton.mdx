import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as SplitButtonStories from './SplitButton.stories';

<Meta of={SplitButtonStories} />

# SplitButton

A split button combines a primary action with a dropdown menu for additional options.

## Props

<Canvas of={SplitButtonStories.Default} />

<Controls of={SplitButtonStories.Default} />

## Variant

A split button can have different visual styles.

<Canvas of={SplitButtonStories.Variant} />

## With Popover

A split button is commonly used with a popover to show additional actions.

<Canvas of={SplitButtonStories.WithPopover} />

## Depth

A split button can have a 3D depth/shadow effect. The shadow can be offset to the right (default) or centered.

<Canvas of={SplitButtonStories.Depth} />
