{
  "name": "dracoui",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --parallel",
    "build:ember": "turbo run build --filter=@dracoui/ember --parallel",
    "build:styles": "turbo run build --filter=@dracoui/styles --parallel"
  },
  "dependencies": {
    "turbo": "^2.4.0"
  },
  "packageManager": "pnpm@9.15.3"
}
