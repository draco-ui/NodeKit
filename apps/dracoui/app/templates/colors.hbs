<section class="border-grid max-w-[1200px] w-full mx-auto flex flex-col lg:flex-row justify-between mt-[40px] space-x-[20px]">
  <div class="flex container-wrapper justify-center items-center">
    <div class="container flex flex-col items-start gap-2 py-8 md:py-10 lg:py-12 max-w-[600px]">
        <Draco::Link
        @iconSize={{15}}
        class="font-[500] pb-[10px] pl-[2px]"
        @iconPosition="trailing"
        @icon="lucide:arrow-right"
        @text="Introducing Draco v0.1"
        @href="https://github.com/corinvo/dracoui"
      >
        <Draco::Mark @size={{14}} />
      </Draco::Link>

      <h1 class="text-2xl font-[500] leading-tight tracking-tighter md:text-4xl">
        Draco Color Library
      </h1>

      <p class="max-w-2xl text-[14px] font-[400] dark:text-[#A1A1A1]">
        Color is a fundamental part of Draco's visual language. In this guide you will <br> learn about the principles, patterns, and abstractions that make up Draco's color system.
      </p>

      <div class="flex gap-2.5 w-full items-start pt-2">
        <Draco::Button @text="Browse Colors" @href="#colors" />
        <Draco::Button @text="View Design Tokens" @route="tokens" @type="secondary" />
      </div>
    </div>
  </div>
</section>

<section id="colors" class="relative border-grid max-w-[1200px] w-full mx-auto flex flex-col lg:flex-row justify-center mt-[40px] space-x-[20px] items-center">
  <span class="absolute top-[39px] left-1/2 transform -translate-x-1/2 w-screen h-px dark:bg-[#272727]" />

  <Draco::Tabs
    @selectedTabIndex={{this.demoSelectedTab}}
    @onClickTab={{this.demoUpdateSelectedTabQueryParam}}
    as |T|
  >
    <T.Tab>
      <h2 class="text-[12px]">Overview</h2>
    </T.Tab>

    {{#each-in this.colors.palette as |tone colorsList|}}
      <T.Tab>
        <h2 class="text-[12px]">{{capitalize (pluralize tone)}}</h2>
      </T.Tab>
    {{/each-in}}

    <T.Panel>
      <div class="grid gap-8 pt-[20px] pb-[100px]">
        {{#each-in this.colors.palette as |tone colorsList|}}
          <Colors::SwatchGroup
            @name={{tone}}
            @colors={{colorsList}}
          />
        {{/each-in}}
      </div>
    </T.Panel>

    {{#each-in this.colors.palette as |tone colorsList|}}
      <T.Panel>
        <div class="grid gap-4 w-full pt-[20px] pb-[100px]">
          {{#each colorsList as |color|}}
            <Colors::Swatch @color={{color}} />
          {{/each}}
        </div>
      </T.Panel>
    {{/each-in}}
  </Draco::Tabs>
</section>
